{
  "name": "hoangsa-cc",
  "version": "0.1.4",
  "type": "module",
  "description": "A lean 3-phase (menu → prepare → cook) context engineering system for Claude Code by Zan.",
  "bin": {
    "hoangsa-cc": "bin/install"
  },
  "files": [
    "bin",
    "templates",
    "README.md",
    "README.vi.md"
  ],
  "keywords": [
    "claude",
    "claude-code",
    "ai",
    "meta-prompting",
    "context-engineering",
    "spec-driven-development"
  ],
  "author": "Zan",
  "license": "MIT",
  "engines": {
    "node": ">=14.18.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unknown-studio-dev/hoangsa.git"
  },
  "homepage": "https://github.com/unknown-studio-dev/hoangsa",
  "bugs": {
    "url": "https://github.com/unknown-studio-dev/hoangsa/issues"
  },
  "scripts": {
    "build": "cargo build --release -p hoangsa-cli",
    "build:media": "cargo build --release -p hoangsa-cli --features media && cp target/release/hoangsa-cli target/release/hoangsa-cli-media",
    "build:all": "npm run build:media && npm run build",
    "build:memory": "cargo build --release -p thoth-cli -p thoth-mcp",
    "build:workspace": "cargo build --release --workspace",
    "test": "cargo build --release -p hoangsa-cli && ./target/release/hoangsa-cli verify $(pwd)",
    "verify": "./target/release/hoangsa-cli verify $(pwd)",
    "format": "prettier --write 'bin/**' '*.json'",
    "format:check": "prettier --check 'bin/**' '*.json'"
  },
  "optionalDependencies": {
    "@hoangsa-cc/cli-linux-x64": "0.1.4",
    "@hoangsa-cc/cli-linux-arm64": "0.1.4",
    "@hoangsa-cc/cli-linux-x64-musl": "0.1.4",
    "@hoangsa-cc/cli-darwin-x64": "0.1.4",
    "@hoangsa-cc/cli-darwin-arm64": "0.1.4",
    "@hoangsa-cc/cli-windows-x64": "0.1.4"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
