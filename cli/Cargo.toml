[package]
name = "hoangsa-cli"
version = "0.0.3"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["formatting", "macros"] }

[profile.release]
strip = true
lto = true
opt-level = "z"
